import { NotFound } from "../ui/components/NotFound"

export const NotFoundPage = () => {
    return <div className="mt-64 w-full flex flex-row items-center justify-center">
        <NotFound />
    </div>
}
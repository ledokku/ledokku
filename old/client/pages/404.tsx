import { NotFound } from '../ui/components/NotFound';

const PageNotFound = () => {
    return (
        <div className="mt-64 w-full flex flex-row items-center justify-center">
            <NotFound />
        </div>
    );
};

export default PageNotFound;

# Migration `20200430164954-format`

This migration has been generated by pradel at 4/30/2020, 4:49:54 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200426160334-add-mysql-to-the-list-of-db..20200430164954-format
--- datamodel.dml
+++ datamodel.dml
@@ -1,25 +1,28 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
 }
 model User {
-  id                      String   @default(uuid()) @id
-  createdAt               DateTime @default(now())
-  updatedAt               DateTime @default(now())
-  username                String   @unique
+  id                      String     @default(uuid()) @id
+  createdAt               DateTime   @default(now())
+  updatedAt               DateTime   @default(now())
+  username                String     @unique
   avatarUrl               String
   email                   String
-  githubId                String   @unique
+  githubId                String     @unique
   githubAccessToken       String
   digitaloceanAccessToken String?
   servers                 Server[]
   sshKeys                 SshKey[]
+  Action                  Action[]
+  App                     App[]
+  Database                Database[]
 }
 model SshKey {
   id             String   @default(uuid()) @id
@@ -32,21 +35,24 @@
   userId         String
 }
 model Server {
-  id          String      @default(uuid()) @id
-  createdAt   DateTime    @default(now())
-  updatedAt   DateTime    @default(now())
-  serverId    Int
-  ip          String?
-  name        String
-  type        ServerTypes
-  status      ServerStatus
-  actionId    Int?
-  user        User        @relation(fields: [userId], references: [id])
-  userId      String
-  sshKey      SshKey      @relation(fields: [sshKeyId], references: [id])
-  sshKeyId    String
+  id        String       @default(uuid()) @id
+  createdAt DateTime     @default(now())
+  updatedAt DateTime     @default(now())
+  serverId  Int
+  ip        String?
+  name      String
+  type      ServerTypes
+  status    ServerStatus
+  actionId  Int?
+  user      User         @relation(fields: [userId], references: [id])
+  userId    String
+  sshKey    SshKey       @relation(fields: [sshKeyId], references: [id])
+  sshKeyId  String
+  Action    Action[]
+  App       App[]
+  Database  Database[]
 }
 enum ServerTypes {
   AWS
@@ -61,17 +67,17 @@
   ARCHIVE
 }
 model Action {
-  id             String   @default(uuid()) @id
-  createdAt      DateTime @default(now())
-  updatedAt      DateTime @default(now())
-  status         ActionStatus
-  type           ActionType
-  server         Server   @relation(fields: [serverId], references: [id])
-  serverId       String
-  user           User     @relation(fields: [userId], references: [id])
-  userId         String
+  id        String       @default(uuid()) @id
+  createdAt DateTime     @default(now())
+  updatedAt DateTime     @default(now())
+  status    ActionStatus
+  type      ActionType
+  server    Server       @relation(fields: [serverId], references: [id])
+  serverId  String
+  user      User         @relation(fields: [userId], references: [id])
+  userId    String
 }
 enum ActionStatus {
   PENDING
```
